// Cards are now fully CSS/SVG — there is no template/back art left to warm
// the cache with. Kept as a no-op so existing call sites don't need changes.
export function preloadCardArt() {
  /* no-op: card frame & back are rendered, not loaded */
}
